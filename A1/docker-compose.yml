services:
  docker-1:
    build:
      context: ./A1-docker-1
      dockerfile: Dockerfile
    ports:
      - 6000:6000
    networks:
      - my-network
    volumes:
      - .:/app

  docker-2:
    build:
      context: ./A1-docker-2
      dockerfile: Dockerfile
    ports:
      - 6001:6001
    networks:
      - my-network
    volumes:
      - .:/app

networks:
  my-network:
    name: my-network
    driver: bridge
